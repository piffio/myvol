language: go

go:
  - 1.8

env:
  - GO_ENV=test

services:
  - postgresql

before_script:
  - psql -c 'create database myvol_test;' -U postgres
  - mkdir -p $TRAVIS_BUILD_DIR/public/assets

go_import_path: github.com/piffio/myvol

install:
  - go get github.com/gobuffalo/buffalo/buffalo
  - go get $(go list ./... | grep -v /vendor/)

script: buffalo test
